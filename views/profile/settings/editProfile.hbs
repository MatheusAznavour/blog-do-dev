{{#if error}}
<div class="alert alert-danger" role="alert">
    {{#each error}}
    {{this}}
    {{/each}}
</div>
{{/if}}

<div class="container mt-5">
    <!-- Login Info -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <span><strong>Login: </strong>userEmail</span>
        
    </div>

    <!-- Form 1 -->
    <form method="post" action="/profile/settings/edit-profile">
        <!-- Nome -->
        <div class="mb-3">
            <label for="nome" class="form-label">name:</label>
            <input name="username"
            required type="text" class="form-control" id="nome" value="E">
        </div>

        <!-- Email de contato -->
        <div class="mb-3">
            <label for="email" class="form-label">Contact email:</label>
            <input name="email" 
            required type="email" class="form-control" id="email" value="matheus.kunn896@gmail.com">
        </div>

        <!-- Description -->
        <div class="mb-3">
            <label for="exampleFormControlTextarea1" class="form-label">Description:</label>
            <textarea name="description" 
            class="form-control" id="exampleFormControlTextarea1" rows="3"></textarea>
        </div>

        <!-- Confirmar Button -->
        <div class="mb-3 text-center">
            <button name="button"
            value="f1" 
            type="submit" class="btn btn-primary">Send</button>
        </div>
    </form>


    <!-- ACADEMIC BACKGROUND -->

    <form method="post" action="/profile/settings/edit-profile">
        <!-- Major -->
        <div class="mb-3">
            <label for="nome" class="form-label">Major:</label>
            <input name="ac_major"
            required type="text" class="form-control" id="nome" value="E">
        </div>

        <!-- Institution -->
        <div class="mb-3">
            <label for="nome" class="form-label">Institution:</label>
            <input name="ac_institution"
            required type="text" class="form-control" id="nome" value="E">
        </div>

        <!-- Arrival date -->
        <div class="mb-3">
            <label for="nome" class="form-label">Arrival date:</label>
            <input name="ac_arrival_date"
            required type="date" class="form-control" id="nome" value="E">
        </div>

        <!-- Arrival date -->
        <div class="mb-3">
            <label for="nome" class="form-label">Departure date:</label>
            <input name="ac_departure_date"
            type="date" class="form-control" id="nome" value="E">
        </div>

        <!-- Description -->
        <div class="mb-3">
            <label for="exampleFormControlTextarea1" class="form-label">Description:</label>
            <textarea name="ac_description"
            class="form-control" id="exampleFormControlTextarea1" rows="3"></textarea>
        </div>

        <!-- Confirmar Button -->
        <div class="mb-3 text-center">
            <button name="button"
            value="f2" 
            type="submit" class="btn btn-primary">Send</button>
        </div>
    </form>

    <!-- PROFISSINAL EXPERIENCE -->
    <form method="post" action="/profile/settings/edit-profile">
        <!-- Position -->
        <div class="mb-3">
            <label for="nome" class="form-label">Position:</label>
            <input
            required type="text" class="form-control" id="nome" value="E">
        </div>

        <!-- Enterprise -->
        <div class="mb-3">
            <label for="nome" class="form-label">Enterprise:</label>
            <input
            required type="text" class="form-control" id="nome" value="E">
        </div>

        <!-- Arrival date -->
        <div class="mb-3">
            <label for="nome" class="form-label">Arrival date:</label>
            <input
            required type="date" class="form-control" id="nome" value="E">
        </div>

        <!-- Arrival date -->
        <div class="mb-3">
            <label for="nome" class="form-label">Departure date:</label>
            <input type="date" class="form-control" id="nome" value="E">
        </div>

        <!-- Description -->
        <div class="mb-3">
            <label for="exampleFormControlTextarea1" class="form-label">Description:</label>
            <textarea class="form-control" id="exampleFormControlTextarea1" rows="3"></textarea>
        </div>

        <!-- Confirmar Button -->
        <div class="mb-3 text-center">
            <button name="button"
            value="f3" 
            type="submit" class="btn btn-primary">Send</button>
        </div>
    </form>

</div>